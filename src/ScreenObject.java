import java.awt.Graphics;

//Interface, for the sake of having an interface, because
//they're cool. :)
//And also because my screen objects share a method. I guess.
public interface ScreenObject {
	
	public void paintYourself(Graphics g);

}
